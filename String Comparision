#include <iostream>

using namespace std;

int main()
{
    char a[20], b[20];
    gets(a);
    gets(b);
    int i,j;
    //String comparision
    for(i=0,j=0;a[i]!='\0'&&b[j]!='\0';i++,j++)
    {
        if(a[i]!=b[j])
            break;
    }
    if(a[i]==b[j])
    cout<<"equal";
    else if(a[i]<b[j])
    cout<<"smaller";
    else cout<<"greater";
    return 0;
}
